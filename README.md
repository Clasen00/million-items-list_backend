# Million Items List — Backend

## Описание
### Высокопроизводительный REST API для управления и оптимизированной работы со списками, содержащими миллионы элементов.

Этот бэкенд-сервис предоставляет эффективные эндпоинты для выборки, сортировки и фильтрации огромных объемов данных. Проект сфокусирован на решении задач, связанных с производительностью при работе с большими списками.

## 🚀 Ключевые особенности
- Оптимизация больших данных: Реализованы механизмы батчинга (пакетной обработки) и дедупликации операций чтения и записи в хранилище для максимальной эффективности.
- Гибкое API: Полноценный REST API с поддержкой пагинации, сортировки и фильтрации данных.


## 🛠 Технологии
- Node.js — серверная среда выполнения.

- Express.js — веб-фреймворк для создания API.

- TypeScript — язык программирования для сборки.

## 📦 Быстрый старт
Клонируйте репозиторий:

```bash
git clone https://github.com/Clasen00/million-items-list_backend.git
cd million-items-list_backend
```

Установите зависимости:
```bash
npm install
```
Запустите сервер для разработки:

```bash
npm run dev
```
Сервер будет запущен (по умолчанию, на `http://localhost:3000`).

## 📚 API
GET /api/items — получение пагинированного списка всех элементов (query-параметры: offset, limit, filter?).

GET /api/selected — получение пагинированного списка выбранных элементов (с учетом порядка из selectedItems).

POST /api/selected — добавление массива ID в выбранные (с проверкой на дубли).

PUT /api/selected/order — обновление порядка выбранных элементов (принимает новый массив selectedItems).

DELETE /api/selected — удаление массива ID из выбранных.

POST /api/items — добавление нового элемента с заданным ID (проверка на уникальность в рамках 1M).
